{
  "seed": 2026,
  "output_dir": "outputs/vlm_smoke_run",
  "data": {
    "source_root": "dataset/anomaly_db_v1",
    "max_train_samples": 256,
    "max_val_samples": 64,
    "max_test_samples": 64,
    "balanced_by_type": true
  },
  "prompt": {
    "system_prompt": "You are a vision-language anomaly grounding model for time-series plots. Read the input PNG plot and return exactly one JSON object. The JSON object must contain two keys: evidence and summary. evidence must be a list. Each item in evidence must contain start, end, type, strength, and direction. type must be one of point, freq, trend, range. If no anomaly is detected, return {\"evidence\": [], \"summary\": \"No anomaly is detected.\"}. Do not output markdown, explanations, or extra text.",
    "user_prompt": "Inspect this plain time-series plot and ground the anomaly. Return one JSON object with structured evidence and one English summary sentence."
  },
  "model": {
    "model_name_or_path": "",
    "trust_remote_code": true
  },
  "training": {
    "mode": "prepare_only",
    "per_device_train_batch_size": 1,
    "per_device_eval_batch_size": 1,
    "gradient_accumulation_steps": 1,
    "learning_rate": 0.00001,
    "num_train_epochs": 1,
    "logging_steps": 10,
    "save_steps": 100,
    "eval_steps": 100,
    "evaluation_strategy": "steps",
    "save_strategy": "steps",
    "gradient_checkpointing": false,
    "bf16": false,
    "fp16": false
  }
}
